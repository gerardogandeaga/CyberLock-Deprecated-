<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".activities.core.ActivityMain"
    tools:ignore="AlwaysShowAction">

    <item
        android:id="@+id/add"
        android:layout_height="wrap_content"
        android:icon="@drawable/ic_add"
        android:title="@string/mnuAdd"
        app:showAsAction="always">

        <menu>
            <item
                android:id="@+id/add_note"
                android:icon="@drawable/ic_note"
                android:orderInCategory="0"
                android:title="@string/mnuAddNote"
                app:showAsAction="never"/>
            <item
                android:id="@+id/add_paymentinfo"
                android:icon="@drawable/ic_card"
                android:orderInCategory="0"
                android:title="@string/mnuAddPaymentInfo"
                app:showAsAction="never"/>
            <item
                android:id="@+id/add_logininfo"
                android:icon="@drawable/ic_login"
                android:orderInCategory="0"
                android:title="@string/mnuAddLoginInfo"
                app:showAsAction="never"/>
        </menu>

    </item>

    <item
        android:id="@+id/options"
        android:layout_height="wrap_content"
        android:icon="@drawable/ic_options"
        android:title="@string/mnuOptions"
        app:showAsAction="always">

        <menu>
            <item
                android:id="@+id/option_options"
                android:icon="@drawable/ic_settings"
                android:orderInCategory="0"
                android:title="@string/mnuOptions"
                app:showAsAction="never"/>
            <item
                android:id="@+id/option_import"
                android:icon="@drawable/ic_import"
                android:orderInCategory="0"
                android:title="@string/mnuImport"
                app:showAsAction="never"/>
        </menu>

    </item>

</menu>
