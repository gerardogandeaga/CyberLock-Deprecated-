<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".activities.core.ActivityMain">

    <item
        android:id="@+id/mnuOptions"
        android:layout_height="wrap_content"
        android:icon="@drawable/ic_options"
        android:title="Options"
        app:showAsAction="always">

        <menu>
            <item
                android:id="@+id/acSettings"
                android:icon="@drawable/ic_settings"
                android:orderInCategory="0"
                android:title="Settings"
                app:showAsAction="never"/>

            <item
                android:id="@+id/acAbout"
                android:orderInCategory="0"
                android:title="About"
                app:showAsAction="never"/>

            <item
                android:icon="@drawable/ic_list_format"
                android:title="List Format">

                <menu>

                    <group
                        android:checkableBehavior="single">

                        <item
                            android:id="@+id/acListLinear"
                            android:icon="@drawable/ic_list_linear"
                            android:title="Linear Format"/>

                        <item
                            android:id="@+id/acListStaggeredGrid"
                            android:icon="@drawable/ic_list_grid"
                            android:title="Grid Format"/>

                    </group>

                </menu>

            </item>

        </menu>

    </item>

</menu>
