<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".core.activities.NoteListActivity"
    tools:ignore="AlwaysShowAction">

    <item
        android:id="@+id/menu_add"
        android:layout_height="wrap_content"
        android:icon="@drawable/ic_add"
        android:title="@string/str_menu_add"
        app:showAsAction="always">

        <menu>

            <item
                android:id="@+id/menu_add_note"
                android:icon="@drawable/ic_ab_note"
                android:orderInCategory="0"
                android:title="@string/str_menu_add_note"
                app:showAsAction="never"/>
            <item
                android:id="@+id/menu_add_card"
                android:icon="@drawable/ic_ab_card"
                android:orderInCategory="0"
                android:title="@string/str_menu_add_card"
                app:showAsAction="never"/>
            <item
                android:id="@+id/menu_add_login"
                android:icon="@drawable/ic_ab_login"
                android:orderInCategory="0"
                android:title="@string/str_menu_add_login"
                app:showAsAction="never"/>
        </menu>

    </item>

    <item
        android:id="@+id/menu_options"
        android:layout_height="wrap_content"
        android:icon="@drawable/ic_options"
        android:title="@string/str_menu_options"
        app:showAsAction="always">

        <menu>
            <item
                android:id="@+id/menu_option_options"
                android:icon="@drawable/ic_ab_cog"
                android:orderInCategory="0"
                android:title="@string/str_menu_options"
                app:showAsAction="never"/>
            <item
                android:id="@+id/menu_option_import"
                android:icon="@drawable/ic_ab_import"
                android:orderInCategory="0"
                android:title="@string/str_menu_import"
                app:showAsAction="never"/>
        </menu>

    </item>

</menu>
